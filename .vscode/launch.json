{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Escuchar en Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "c:/wamp64/www/escorpion": "${workspaceFolder}"
            }
        }
    ]
}